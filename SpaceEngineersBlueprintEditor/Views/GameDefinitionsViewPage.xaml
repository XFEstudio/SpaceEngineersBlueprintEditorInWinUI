<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SpaceEngineersBlueprintEditor.Views.GameDefinitionsViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceEngineersBlueprintEditor.Views"
    xmlns:model="using:SpaceEngineersBlueprintEditor.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnDefinitions="5*, *" RowSpacing="20">
        <GridView Canvas.ZIndex="0" ItemsSource="{x:Bind ViewModel.Definitions}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="model:DefinitionViewData">
                    <Grid Width="80" Height="80" RowDefinitions="50, Auto">
                        <Image Source="{x:Bind ImageSource}" Width="50" Height="50" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Row="1" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" Text="{x:Bind DefinitionBase.DisplayNameText}"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <AutoSuggestBox Margin="0,0,0,20" Width="250" PlaceholderText="Search definitions..." QueryIcon="Find" Canvas.ZIndex="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Text="{x:Bind ViewModel.SearchText}"/>
    </Grid>
</Page>